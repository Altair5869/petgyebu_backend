# 프로젝트 지침

이 파일은 `CLAUDE.md`이며, `AGENTS.md`는 여기를 가리키는 심볼릭 링크다. Claude Code와 Codex가 같은 지침을 읽는다. 내용을 고칠 때는 이 파일만 고치면 된다.

## 응답 스타일

이 프로젝트에서는 항상 `caveman` 스킬 (full 레벨) 켜고 응답. 세션 시작마다 자동 적용, 사용자가 "stop caveman" / "normal mode" 말할 때까지 유지.

## 코딩 가이드라인

이 프로젝트에서 코드를 작성/수정/리뷰할 때는 `karpathy-guidelines` 스킬을 따른다.

- 가정을 명시하고, 불확실하면 진행 전에 질문한다.
- 요청받은 것 이상의 기능·추상화·유연성을 추가하지 않는다 (최소 구현).
- 기존 코드는 요청과 직접 관련된 부분만 수정한다 (외과적 변경). 무관한 리팩터링·포맷 정리 금지.
- 내 변경으로 생긴 고아 코드(미사용 import/변수/함수)만 제거하고, 기존 데드 코드는 언급만 하고 삭제하지 않는다.
- 작업 전에 검증 가능한 성공 기준(테스트 등)을 정의하고, 그 기준으로 확인 후 완료로 간주한다.

## 트러블슈팅 기록 (자동 갱신)

디버깅이나 오류 해결이 끝나면 **사용자가 요청하지 않아도** `docs/11-troubleshooting-log.md`에 항목을 추가한다. 포트폴리오 자료로 쓰이므로 진단 과정이 드러나야 한다.

**기록하는 것**

- 원인을 찾는 데 한 단계 이상 추론이 필요했던 문제
- 겉보기 성공 뒤에 숨은 실패(빌드 통과인데 기능이 비어 있는 등)
- 라이브러리 좌표·패키지·API가 문서나 관례와 달랐던 경우
- 보안 문제와 그 수정
- 수정했는데 원래 증상이 남아 한 번 더 파고들어야 했던 경우
- 검증 방법 자체의 오류, 작업 중 낸 실수

**기록하지 않는 것**

- 오타, 컴파일 에러처럼 원인이 즉시 보이는 것
- 기능 구현 자체(그건 PR 본문과 `_workspace/` 요약의 몫)
- 해결하지 못한 것은 트러블슈팅이 아니라 백로그 Task로 등록한다

**항목 형식** — 기존 항목과 같은 구조를 따른다.

1. 증상 (실제 에러 메시지나 관측된 동작을 그대로)
2. 진단 (어떤 단서로 원인에 도달했는지. 오판했다면 그것도 남긴다)
3. 원인
4. 해결
5. 검증 (가능하면 **양방향** — 고친 뒤 통과만이 아니라, 되돌리면 다시 깨지는지)
6. 배운 것 (한 줄로 끝나면 생략)

**갱신 절차**

- 요약 표에 행을 추가하고 관련 PR 번호를 단다. PR 번호는 실제 병합 이력과 대조해 확인한다
- 분류별 건수·"조용한 실패" 건수 등 문서 안의 숫자가 어긋나지 않는지 확인한다
- 회고 절에 새 교훈이 생겼으면 함께 갱신한다
- 문서 변경은 그 문제를 고친 PR에 함께 넣는다. 별도 PR로 미루지 않는다

## 하네스: petgyebu 백엔드 스프린트 구현

**목표:** `docs/06-sprint-plan.md` 순서대로 백엔드 기능을 구현하고 통합 정합성을 검증한다.

**트리거:** 스프린트 구현, 기능(F-XXXXX) 구현, 스프린트 재작업/QA 반영 등 요청 시 `petgyebu-sprint` 스킬을 사용하라. 단순 질문이나 국소적 코드 수정은 직접 응답 가능.

**변경 이력:**
| 날짜 | 변경 내용 | 대상 | 사유 |
|------|----------|------|------|
| 2026-09-05 | 초기 구성 (backend-engineer + qa-reviewer 2인 팀, 생성-검증 패턴) | 전체 | 스프린트 기반 백엔드 구현 자동화 |
| 2026-09-07 | 브랜치 전략 반영 (기능 단위 브랜치, squash merge, push/PR은 사용자 확인 후 실행) | petgyebu-sprint, backend-engineer, docs/07-branch-strategy.md | GitHub 브랜치 전략 수립 |
| 2026-09-17 | `AGENTS.md`를 `CLAUDE.md`를 가리키는 심볼릭 링크로 전환 | CLAUDE.md, AGENTS.md | 두 파일이 제목 한 줄 외에 동일해 한쪽만 고치면 두 하네스가 다르게 동작할 위험이 있었다 |
| 2026-09-18 | 트러블슈팅 기록 자동 갱신 규칙 추가 | CLAUDE.md, docs/11-troubleshooting-log.md | 포트폴리오 자료로 쓰이므로 사용자가 요청하지 않아도 해결 시점에 바로 남겨야 한다 |
